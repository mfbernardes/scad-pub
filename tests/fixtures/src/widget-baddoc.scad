// Fixture design whose `// @doc` annotation names a file that doesn't exist,
// so gen-schema's mustExist check fails the build (tests/gen-schema.test.mjs).
// @doc no-such-doc.md

/* [Main] */
label = "hi";
